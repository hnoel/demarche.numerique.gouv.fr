---
name: Create and publish a Docker image

# Configures this workflow to run manually or every time a change is pushed to the branch called `master`.
on:
  workflow_dispatch: {}
  push:
    branches: ['main']

# Defines two custom environment variables for the workflow. These are used for the Container registry domain, and a name for the Docker image that this workflow builds.
env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

# There is a single job in this workflow. It's configured to run on the latest available version of Ubuntu.
jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    # Sets the permissions granted to the `GITHUB_TOKEN` for the actions in this job.
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    # required at step 19 ?
    services:
      postgres:
        image: postgis/postgis:17-3.5
        env:
          POSTGRES_USER: tps_development
          POSTGRES_DB: tps
          POSTGRES_PASSWORD: tps_development
        ports: [ "5432:5432" ]      #
    steps:
      - name: 'Checkout repo "https://github.com/demarche-numerique/demarche.numerique.gouv.fr"'
        uses: actions/checkout@v6
        with:
          repository: 'demarche-numerique/demarche.numerique.gouv.fr'
      - name: 'Checkout repo "https://github.com/hnoel/demarche.numerique.gouv.fr" into "./hnoel"'
        uses: actions/checkout@v6
        with:
          repository: ${{ github.repository }}
          path: './hnoel'
      - name: Merge checkout repositories
        run: cp -r ./hnoel/* ./ && mv ./env.example ./.env && rm -rf ./hnoel
        shell: bash
      - name: echo postgres container ip
        id: postgres_ip
        run: echo "POSTGRES=$(hostname -I | awk '{print $1}')" >> "$GITHUB_OUTPUT"
        # Uses the `docker/login-action` action to log in to the Container registry registry using the account and password that will publish the packages. Once published, the packages are scoped to the account defined here.
      - name: Log in to the Container registry
        uses: docker/login-action@65b78e6e13532edd9afa3aa52ac7964289d1a9c1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      # This step uses [docker/metadata-action](https://github.com/docker/metadata-action#about) to extract tags and labels that will be applied to the specified image. The `id` "meta" allows the output of this step to be referenced in a subsequent step. The `images` value provides the base name for the tags and labels.
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@9ec57ed1fcdbf14dcef7dfbe97b2010124a938b7
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
      # This step uses the `docker/build-push-action` action to build the image, based on your repository's `Dockerfile`. If the build succeeds, it pushes the image to GitHub Packages.
      # It uses the `context` parameter to define the build's context as the set of files located in the specified path. For more information, see [Usage](https://github.com/docker/build-push-action#usage) in the README of the `docker/build-push-action` repository.
      # It uses the `tags` and `labels` parameters to tag and label the image with the output from the "meta" step.
      # - name: Set up QEMU
      #   uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push
        uses: docker/build-push-action@v6
        id: push
        with:
          add-hosts: postgres:${{ steps.postgres_ip.outputs.POSTGRES }}
          file: ./Dockerfile
          platforms: linux/amd64
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          sbom : true
        env:
          SOURCE_DATE_EPOCH: 0
          ACTIVE_STORAGE_SERVICE: ''
          AGENT_CONNECT_ENABLED: ''
          AGENT_CONNECT_ID: ''
          AGENT_CONNECT_SECRET: ''
          AGENT_CONNECT_BASE_URL: ''
          AGENT_CONNECT_JWKS: ''
          AGENT_CONNECT_REDIRECT: ''
          API_ADRESSE_URL: ''
          API_COJO_URL: ''
          API_CPS_AUTH: ''
          API_CPS_CLIENT_ID: ''
          API_CPS_CLIENT_SECRET: ''
          API_CPS_PASSWORD: ''
          API_CPS_URL: ''
          API_CPS_USERNAME: ''
          API_EDUCATION_URL: ''
          API_ENTREPRISE_DEFAULT_SIRET: ''
          API_ENTREPRISE_KEY: ''
          API_ISPF_AUTH_URL: ''
          API_ISPF_URL: ''
          API_ISPF_PASSWORD: ''
          API_ISPF_USER: ''
          APPLICATION_BASE_URL: ''
          APPLICATION_NAME: ''
          APP_HOST: ''
          APP_NAME: ''
          AR_ENCRYPTION_KEY_DERIVATION_SALT: ''
          AR_ENCRYPTION_PRIMARY_KEY: ''
          BASIC_AUTH_ENABLED: ''
          BASIC_AUTH_PASSWORD: ''
          BASIC_AUTH_USERNAME: ''
          CARRIERWAVE_CACHE_DIR: ''
          CLAMAV_ENABLED: ''
          COJO_JWT_RSA_PRIVATE_KEY: ''
          CRISP_CLIENT_KEY: ''
          CRISP_ENABLED: ''
          DB_DATABASE: tps
          DB_HOST: postgres
          DB_PASSWORD: tps_development
          DB_POOL: ''
          DB_USERNAME: tps_development
          DB_PORT: 5432
          UNIVERSIGN_API_URL: ''
          UNIVERSIGN_USERPWD: ''
          API_GEO_DEGRADED_MODE: ''
          API_GEO_URL: ''
          DEMANDE_INSCRIPTION_ADMIN_PAGE_URL: ''
          DOLIST_BALANCING_VALUE: ''
          DOLIST_USERNAME: ''
          DOLIST_PASSWORD: ''
          DOLIST_ACCOUNT_ID: ''
          DOLIST_API_KEY: ''
          DOC_URL: ''
          DS_PROXY_URL: ''
          ENCRYPTION_SERVICE_SALT: ''
          FACEBOOK_CLIENT_ID: ''
          FACEBOOK_CLIENT_SECRET: ''
          FAVICON_16PX_SRC: ''
          FAVICON_32PX_SRC: ''
          FAVICON_96PX_SRC: ''
          FC_PARTICULIER_BASE_URL: ''
          FC_PARTICULIER_ID: ''
          FC_PARTICULIER_SECRET: ''
          FOG_DIRECTORY: ''
          FOG_ENABLED: ''
          FOG_OPENSTACK_API_KEY: ''
          FOG_OPENSTACK_AUTH_URL: ''
          FOG_OPENSTACK_IDENTITY_API_VERSION: ''
          FOG_OPENSTACK_REGION: ''
          FOG_OPENSTACK_TENANT: ''
          FOG_OPENSTACK_URL: ''
          FOG_OPENSTACK_USERNAME: ''
          GITHUB_CLIENT_ID: ''
          GITHUB_CLIENT_SECRET: ''
          GOOGLE_CLIENT_ID: ''
          GOOGLE_CLIENT_SECRET: ''
          HELPSCOUT_CLIENT_ID: ''
          HELPSCOUT_CLIENT_SECRET: ''
          HELPSCOUT_MAILBOX_ID: ''
          HELPSCOUT_WEBHOOK_SECRET: ''
          INVISIBLE_CAPTCHA_SECRET: ''
          LEGIT_ADMIN_DOMAINS: ''
          LOGRAGE_ENABLED: ''
          LOGRAGE_SOURCE: ''
          MAILCATCHER_ENABLED: ''
          MAILCATCHER_HOST: ''
          MAILCATCHER_PORT: ''
          MAILER_LOGO_SRC: ''
          MAILJET_API_KEY: ''
          MAILJET_SECRET_KEY: ''
          MAILTRAP_ENABLED: ''
          MAILTRAP_PASSWORD: ''
          MAILTRAP_USERNAME: ''
          MATOMO_ENABLED: ''
          MATOMO_COOKIE_DOMAIN: ''
          MATOMO_DOMAIN: ''
          MATOMO_HOST: ''
          MATOMO_ID: ''
          MATOMO_IFRAME_URL: ''
          MICROSOFT_CLIENT_ID: ''
          MICROSOFT_CLIENT_SECRET: ''
          OTP_SECRET_KEY: ''
          PATH: ''
          PIPEDRIVE_KEY: ''
          PROCEDURE_DEFAULT_LOGO_SRC: ''
          RAILS_ENV: production
          RAILS_LOG_TO_STDOUT: ''
          RAILS_SERVE_STATIC_FILES: ''
          RUBY_YJIT_ENABLE: ''
          SAML_IDP_ENABLED: ''
          SAML_IDP_CERTIFICATE: ''
          SAML_IDP_SECRET_KEY: ''
          SECRET_KEY_BASE: '' 
          S3_ENDPOINT: '' 
          S3_BUCKET: ''
          S3_ACCESS_KEY: ''  
          S3_SECRET_KEY: ''
          S3_REGION: ''
          SENDINBLUE_API_V3_KEY: ''
          SENDINBLUE_BALANCING_VALUE: ''
          SENDINBLUE_CLIENT_KEY: ''
          SENDINBLUE_LOGIN_URL: ''
          SENDINBLUE_SMTP_KEY: ''
          SENDINBLUE_USER_NAME: ''
          SENTRY_CURRENT_ENV: ''
          SENTRY_DSN_JS: ''
          SENTRY_DSN_RAILS: ''
          SENTRY_ENABLED: ''
          SIGNING_KEY: ''
          SIPF_CLIENT_BASE_URL: ''
          SIPF_CLIENT_ID: ''
          SIPF_CLIENT_SECRET: ''
          SKYLIGHT_AUTHENTICATION_KEY: ''
          SKYLIGHT_DISABLE_AGENT: ''
          SOURCE: ''
          STRICT_EMAIL_VALIDATION_STARTS_ON: ''
          TATOU_BASE_URL: ''
          TATOU_CLIENT_ID: ''
          TATOU_CLIENT_SECRET: ''
          TRUSTED_NETWORKS: ''
          CERTIGNA_USERPWD: ''
          WATERMARK_FILE: ''
          WEASYPRINT_URL: ''
          YAHOO_CLIENT_ID: ''
          YAHOO_CLIENT_SECRET: ''
      # This step generates an artifact attestation for the image, which is an unforgeable statement about where and how it was built. It increases supply chain security for people who consume the image. For more information, see [Using artifact attestations to establish provenance for builds](/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds).
      #- name: Generate artifact attestation
      #  uses: actions/attest-build-provenance@v2
      #  with:
      #    subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME}}
      #    subject-digest: ${{ steps.push.outputs.digest }}
      #    push-to-registry: true
